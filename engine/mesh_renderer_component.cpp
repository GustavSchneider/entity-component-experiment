#include "mesh_renderer_component.hpp"

void mesh_renderer_component::init() {
    //init program and stuff
}
void  mesh_renderer_component::update(float dt) {
    // update time in program or something, it might be a bit dumb
    // to do this here though, multiple components will probably
    // use the same program and stuff.
    // but where should the program uniform be updated then?
}
